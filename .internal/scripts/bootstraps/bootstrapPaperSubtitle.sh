#!/usr/bin/env bash
export BOOTSTRAP_QUESTION="Please provide the subtitle of your paper (can be empty)"
export BOOTSTRAP_FORMAT="${ARG_COLOR}SUBTITLE${RESET}"
export BOOTSTRAP_EXAMPLES=("A deep dive into why docker is awesome")
